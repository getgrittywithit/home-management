'use client'

// Per-kid health summary cards, recent requests, flagged concerns
export default function HealthOverview() {
  return (
    <div className="p-4 text-center text-gray-400">
      <p>Per-kid health summary cards, recent requests, flagged concerns — content will be migrated from parent component</p>
    </div>
  )
}
